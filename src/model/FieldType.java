package model;

/**
 * Enum representing the different types of fields on the board.
 *
 * @author Mick Kohler
 */
public enum FieldType {
    PATH,
    VILLAGE_PATH,
    OBSTACLE,
    VILLAGE_OBSTACLE,
    TARGET,
    FOREST,
    SAFEZONE,
    START,
    EMPTY;

}